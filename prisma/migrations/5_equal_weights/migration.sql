-- Все типы работ считаются в среднем балле одинаково: удвоение контрольной
-- убрано и для уже выставленных оценок, чтобы история считалась так же,
-- как новые записи. Поле weight остаётся (сейчас всегда 1) — механизм
-- взвешенного среднего сохранён на будущее.

UPDATE "Grade" SET "weight" = 1 WHERE "weight" <> 1;
