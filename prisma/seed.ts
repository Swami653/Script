/**
 * Локальная подготовка базы. Делает то же, что и сборка на Vercel:
 * при необходимости очищает данные (RESET_DATA=true) и создаёт администратора
 * из ADMIN_USERNAME / ADMIN_PASSWORD.
 *
 * Демонстрационных учеников и оценок больше нет — журнал наполняется
 * реальными данными через панель администратора.
 */
import "./bootstrap";
